# Additional packages for VGGish, CLAP, Whisper (beyond requirements.txt)
# Install: pip install -r requirements-extractors.txt

tensorflow>=2.15.0
tensorflow-hub>=0.16.0
msclap>=1.3.0
torchaudio>=2.11.0,<2.12.0

# CLAP on macOS also needs FFmpeg (e.g. conda install -c conda-forge ffmpeg).
# See TUNI_emotion_data_extraction/README.md for torchcodec / symlink notes.
